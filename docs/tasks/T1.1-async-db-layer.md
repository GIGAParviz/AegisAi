# T1.1 — لایه دیتابیس async

| | |
|---|---|
| **فاز** | 1 — Gateway & Security |
| **روز برنامه** | Day 3 (2026-08-31) |
| **وابستگی** | T0.3 |
| **مبحث Learn مرتبط** | SQLAlchemy 2.0 async: engine, sessionmaker, unit-of-work |
| **وضعیت** | ⬜ TODO |

## هدف
engine و session ساز async از روی settings تا فازهای مدل/API فقط وابسته به این لایه باشند.

## گام‌ها
1. app/db/engine.py: ساخت create_async_engine(settings.database_url) و async_sessionmaker(expire_on_commit=False).
2. app/db/base.py: class Base(DeclarativeBase) با naming_convention برای constraints.
3. tests/test_db.py: با sqlite+aiosqlite در حافظه، جدول تستی بساز، session باز کن، insert/select و commit را تست کن.
4. commit.

## فایل‌ها
- `app/db/engine.py`
- `app/db/base.py`
- `tests/test_db.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_db.py -q
```
انتظار: 1 passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
