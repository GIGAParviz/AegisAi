# T5.3 — Human-in-the-loop approvals

| | |
|---|---|
| **فاز** | 5 — Agentic Runtime |
| **روز برنامه** | Day 23 (2026-09-20) |
| **وابستگی** | T5.1 |
| **مبحث Learn مرتبط** | Human-in-the-loop: interrupt/resume, approval UX |
| **وضعیت** | ⬜ TODO |

## هدف
اکشن های حساس گراف را متوقف کنند تا تأیید انسانی بیاید؛ همه در audit.

## گام‌ها
1. app/db/models/approval.py: id، graph_run_id، proposal_json، status (pending/approved/rejected)، decided_by، created_at.
2. برخی اکشن ها settings.requires_approval=True → node gate به جای deny/allow، interrupt صادر کند.
3. app/api/approvals.py: POST /approvals/{id}/decision با نقش admin؛ resume گراف با پاسخ.
4. tests/test_approvals.py: interrupt→pending، تأیید→ادامه، رد→respond با پیام.
5. commit.

## فایل‌ها
- `app/db/models/approval.py`
- `app/api/approvals.py`
- `tests/test_approvals.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_approvals.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
