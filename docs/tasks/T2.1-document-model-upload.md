# T2.1 — مدل Document + API آپلود

| | |
|---|---|
| **فاز** | 2 — Data Engine |
| **روز برنامه** | Day 8 (2026-09-05) |
| **وابستگی** | T1.2 |
| **مبحث Learn مرتبط** | RAG pipeline end-to-end map + extraction formats |
| **وضعیت** | ⬜ TODO |

## هدف
ثبت و آپلود سند با وضعیت lifecycle (queued تا ready) که پایپلین ingest رویش سوار شود.

## گام‌ها
1. app/db/models/document.py: id، filename، mime، status (enum: queued/processing/ready/failed)، error، owner_id، created_at.
2. app/api/documents.py: POST /documents (multipart، ذخیره فایل در uploads/، رکورد queued، پاسخ 202) و GET /documents/{id}.
3. migration جدید برای جدول documents.
4. tests/test_documents.py: آپلود → 202 + رکورد queued؛ GET همان سند.
5. commit.

## فایل‌ها
- `app/db/models/document.py`
- `app/api/documents.py`
- `tests/test_documents.py`

## Verify
```bash
cd /e/projs/AegisAI && alembic upgrade head && python -m pytest tests/test_documents.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
