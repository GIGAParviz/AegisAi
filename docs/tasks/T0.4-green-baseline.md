# T0.4 — بیس لاین سبز: pytest + ruff + commit

| | |
|---|---|
| **فاز** | 0 — Foundation |
| **روز برنامه** | Day 2 (2026-08-30) |
| **وابستگی** | T0.3 |
| **مبحث Learn مرتبط** | pytest essentials (اجرای کامل suite) + ruff |
| **وضعیت** | ⬜ TODO |

## هدف
پروژه در نقطه ای تضمین شده سبز برسد: همه تست ها pass و لینت تمیز؛ مبنای CI روز ۲.

## گام‌ها
1. `python -m ruff check .` را اجرا کن و خطاها را اصلاح کن (در صورت نیاز [tool.ruff] با line-length=100 به pyproject اضافه کن).
2. `python -m pytest -q` سبز باشد.
3. اگر .venv نداری با uv بساز: `uv venv && uv pip install -e ".[dev]"`.
4. commit نهایی روز.

## فایل‌ها
- `pyproject.toml`
- `.gitignore`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest -q && python -m ruff check .
```
انتظار: all passed + no ruff errors

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
