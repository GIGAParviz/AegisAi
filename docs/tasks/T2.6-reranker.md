# T2.6 — Reranker (اختیاری)

| | |
|---|---|
| **فاز** | 2 — Data Engine |
| **روز برنامه** | Day 13 (2026-09-10) |
| **وابستگی** | T2.5 |
| **مبحث Learn مرتبط** | Hybrid search: dense vs sparse, RRF fusion, top-k tuning |
| **وضعیت** | ⬜ TODO |

## هدف
بازرتبه بندی top-20 با cross-encoder و انتخاب top-5؛ خاموش/روشن با settings.

## گام‌ها
1. app/services/reranker.py: CrossEncoderReranker با مدل سبک (ms-marco-MiniLM) روی CPU.
2. در vector_store پس از hybrid_search، اگر settings.rerank_enabled=True ریرنک انجام شود.
3. tests: با fake scores ترتیب درست را چک کن (مدل واقعی لازم نیست).
4. commit.

## فایل‌ها
- `app/services/reranker.py`
- `tests/test_reranker.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_reranker.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
