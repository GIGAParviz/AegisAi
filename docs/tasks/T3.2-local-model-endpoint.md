# T3.2 — راه اندازی مدل محلی (llama.cpp) یا API ریموت

| | |
|---|---|
| **فاز** | 3 — Reasoning Engine |
| **روز برنامه** | Day 15 (2026-09-12) |
| **وابستگی** | T3.1 |
| **مبحث Learn مرتبط** | GGUF quantization + VRAM math for 4GB (KV cache, context length) |
| **وضعیت** | ⬜ TODO |

## هدف
یک endpoint سازگار با OpenAI روی سیستم با 4GB VRAM: llama-server با GGUF کوانتیزه، یا در نبود آن API ریموت.

## گام‌ها
1. دانلود یک GGUF مناسب 4GB (Q4_K_M از مدل 3B مثل Qwen2.5-3B-Instruct یا Phi-3-mini؛ 7B فقط اگر CPU offload قبول باشد).
2. اجرای llama-server با --n-gpu-layers مناسب و چک curl http://localhost:8080/v1/models.
3. settings: llm_provider=local، llm_base_url=http://localhost:8080/v1، llm_model=<نام مدل>.
4. تست integration اختیاری علامت دار (skip اگر سرور up نیست): complete ساده.
5. commit + ثبت مدل انتخابی و n_ctx تجربی در PROGRESS.

## فایل‌ها
- `(سرویس بیرونی) app/core/config.py`

## Verify
```bash
curl http://localhost:8080/v1/models
```
انتظار: لیست مدل با نام GGUF انتخابی

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
