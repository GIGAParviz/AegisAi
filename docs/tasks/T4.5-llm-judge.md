# T4.5 — LLM-as-a-Judge (اختیاری)

| | |
|---|---|
| **فاز** | 4 — Harness |
| **روز برنامه** | Day 20 (2026-09-17) |
| **وابستگی** | T3.1، T4.4 |
| **مبحث Learn مرتبط** | (ادامه Day 20) quality scoring با LLM-as-a-Judge |
| **وضعیت** | ⬜ TODO |

## هدف
سنجش کیفیت پاسخ نهایی نسبت به context مستند؛ پرچم settings و ورودی به evals فاز ۶.

## گام‌ها
1. app/services/judge.py: score(answer, context) با rubric ساده (groundedness 1-5) به صورت JSON خروجی سخت گیرانه.
2. خاموش به صورت پیش فرض (settings.judge_enabled=False).
3. tests با FakeProvider: خروجی valid parse شود؛ خروجی خراب رد.
4. commit.

## فایل‌ها
- `app/services/judge.py`
- `tests/test_judge.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_judge.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
