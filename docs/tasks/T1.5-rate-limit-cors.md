# T1.5 — Rate limiting (Redis) + CORS

| | |
|---|---|
| **فاز** | 1 — Gateway & Security |
| **روز برنامه** | Day 7 (2026-09-04) |
| **وابستگی** | T1.3 |
| **مبحث Learn مرتبط** | Rate-limiting algorithms: fixed/sliding window, token bucket |
| **وضعیت** | ⬜ TODO |

## هدف
محدودسازی نرخ درخواست با پنجره ثابت روی Redis و fallback در حافظه ای وقتی Redis نیست + CORS از settings.

## گام‌ها
1. app/api/middleware/rate_limit.py: کلید per-client، INCR + EXPIRE روی Redis، 429 با Retry-After؛ اگر redis در دسترس نبود دیکشنری درون حافظه ای (برای تست و dev).
2. CORS middleware با origins از settings.
3. tests/test_rate_limit.py: با fake Redis، درخواست N+1ام → 429.
4. commit.

## فایل‌ها
- `app/api/middleware/rate_limit.py`
- `tests/test_rate_limit.py`
- `app/main.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_rate_limit.py -q
```
انتظار: passed (429 بعد از سقف)

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
