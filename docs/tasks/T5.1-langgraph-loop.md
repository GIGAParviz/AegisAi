# T5.1 — حلقه ایجنت با LangGraph

| | |
|---|---|
| **فاز** | 5 — Agentic Runtime |
| **روز برنامه** | Day 21 (2026-09-18) |
| **وابستگی** | T4.3، T3.1 |
| **مبحث Learn مرتبط** | LangGraph: state machines, edges, checkpointing |
| **وضعیت** | ⬜ TODO |

## هدف
state machine کامل: reason→propose→gate→execute→verify→respond با checkpoint در Postgres.

## گام‌ها
1. app/agents/graph.py: TypedDict AgentState (messages، proposals، results، status).
2. node ها: reason (LLM)، propose (parse)، gate (Policy Gate)، execute (Action registry)، verify (Verifier)، respond.
3. edges شرطی: deny→respond با پیام policy violation؛ repair→reason.
4. checkpointer با AsyncPostgresSaver (یا MemorySaver برای تست).
5. tests/test_agent_graph.py: مسیر کامل با FakeProvider و اکشن مجاز؛ مسیر deny.
6. commit.

## فایل‌ها
- `app/agents/graph.py`
- `app/agents/__init__.py`
- `tests/test_agent_graph.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_agent_graph.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
