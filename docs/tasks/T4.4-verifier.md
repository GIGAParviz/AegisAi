# T4.4 — لایه Verify (schema + قواعد + retry)

| | |
|---|---|
| **فاز** | 4 — Harness |
| **روز برنامه** | Day 20 (2026-09-17) |
| **وابستگی** | T4.3 |
| **مبحث Learn مرتبط** | Output validation + retry-with-feedback repair loops |
| **وضعیت** | ⬜ TODO |

## هدف
خروجی ابزار/مدل قبل از پذیرش، اعتبارسنجی سخت گیرانه شود و خطا به مدل بازخورد داده شود (حداکثر ۲ بار).

## گام‌ها
1. app/services/verifier.py: verify(output, schema, rules) -> VerifyResult؛ قواعد determinstic (مثلا مقادیر خالی، محدوده ها).
2. حلقه retry: خطا + توضیح قاعده در پیام بعدی به مدل؛ max_retries=2 از settings.
3. ادغام در جریان اکشن: بعد از اجرای ابزار، خروجی verify شود.
4. tests/test_verifier.py: خروجی نامعتبر → repair prompt ساخته می شود؛ بار سوم معتبر → قبول؛ شکست نهایی → خطای ساختاریافته.
5. commit.

## فایل‌ها
- `app/services/verifier.py`
- `tests/test_verifier.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_verifier.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
