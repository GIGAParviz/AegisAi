# T4.3 — Policy Gate (deny-by-default + audit)

| | |
|---|---|
| **فاز** | 4 — Harness |
| **روز برنامه** | Day 19 (2026-09-16) |
| **وابستگی** | T4.2، T1.4 |
| **مبحث Learn مرتبط** | Policy patterns: deny-by-default, allowlists, audit trails |
| **وضعیت** | ⬜ TODO |

## هدف
هیچ اکشنی بدون مجوز صریح اجرا نشود؛ هر تصمیم allow/deny در جدول audit ثبت و غیرقابل تغییر باشد.

## گام‌ها
1. app/db/models/policy_audit.py: id، actor، action، decision (allow/deny)، reason، proposal_json، created_at.
2. app/policies/gate.py: check(proposal, user) -> Decision؛ قواعد: عضویت در registry، min_role، constraints پارامترها، سقف نرخ؛ default=deny.
3. عدم ثبت در audit = خطا؛ حتی deny ها ثبت شوند.
4. tests/test_policy_gate.py: اکشن ناشناس→deny+audit، نقش ناکافی→deny، مجاز→allow+audit، پارامتر خارج از constraint→deny.
5. commit.

## فایل‌ها
- `app/db/models/policy_audit.py`
- `app/policies/gate.py`
- `tests/test_policy_gate.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_policy_gate.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
