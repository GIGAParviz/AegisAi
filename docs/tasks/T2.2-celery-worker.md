# T2.2 — Celery worker (broker=Redis)

| | |
|---|---|
| **فاز** | 2 — Data Engine |
| **روز برنامه** | Day 9 (2026-09-06) |
| **وابستگی** | T2.1 |
| **مبحث Learn مرتبط** | Celery semantics: idempotency, retries, eager-mode testing |
| **وضعیت** | ⬜ TODO |

## هدف
پردازش background برای ingest تا API بلاک نشود؛ پایه OCR/embedding آینده.

## گام‌ها
1. app/workers/celery_app.py: Celery با broker/backend از settings.redis_url.
2. task ingest_document(document_id): status→processing، بعد (فعلا stub) →ready؛ در خطا status=failed + error.
3. tests/test_worker.py با task_always_eager=True: صدا زدن task وضعیت سند را عوض کند.
4. commit.

## فایل‌ها
- `app/workers/celery_app.py`
- `app/workers/tasks.py`
- `tests/test_worker.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_worker.py -q
```
انتظار: passed (eager mode)

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
