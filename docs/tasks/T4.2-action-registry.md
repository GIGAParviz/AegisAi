# T4.2 — رجیستری اکشن ها (Pydantic)

| | |
|---|---|
| **فاز** | 4 — Harness |
| **روز برنامه** | Day 18 (2026-09-15) |
| **وابستگی** | T0.3 |
| **مبحث Learn مرتبط** | Tool/function calling: JSON schemas, strict parsing |
| **وضعیت** | ⬜ TODO |

## هدف
هر tool قابل پیشنهاد توسط LLM یک schema سخت گیرانه داشته باشد؛ parse پیشنهاد مدل با رد صریح.

## گام‌ها
1. app/policies/actions.py: BaseModel برای هر اکشن (مثل search_docs، get_document) + ActionProposal با discriminated union.
2. ثبت در ActionRegistry: name -> (schema, min_role, constraints).
3. parse_proposal(raw_json): خطای مشخص برای JSON بد، نام ناشناس، پارامتر اضافی.
4. tests/test_actions.py: parse موفق، رد JSON بد، رد نام ناشناس، رد پارامتر غیرمجاز.
5. commit.

## فایل‌ها
- `app/policies/actions.py`
- `app/policies/__init__.py`
- `tests/test_actions.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_actions.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
