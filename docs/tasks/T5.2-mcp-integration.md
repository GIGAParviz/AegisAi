# T5.2 — کلاینت MCP + اولین tool server

| | |
|---|---|
| **فاز** | 5 — Agentic Runtime |
| **روز برنامه** | Day 22 (2026-09-19) |
| **وابستگی** | T5.1، T4.2 |
| **مبحث Learn مرتبط** | MCP: tools/resources/prompts, stdio vs HTTP transports |
| **وضعیت** | ⬜ TODO |

## هدف
اتصال استاندارد ایجنت به ابزارهای بیرونی؛ ابزارهای MCP در Action registry ثبت شوند.

## گام‌ها
1. وابستگی mcp (official SDK) اضافه شود.
2. app/agents/mcp_client.py: اتصال stdio به یک سرور ساده (filesystem) + لیست tools.
3. bridge: هر MCP tool → schema در ActionRegistry (min_role و constraints امن).
4. tests/test_mcp.py: با سرور MCP فیک (in-process): discovery + فراخوانی.
5. commit.

## فایل‌ها
- `app/agents/mcp_client.py`
- `tests/test_mcp.py`
- `pyproject.toml`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_mcp.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
