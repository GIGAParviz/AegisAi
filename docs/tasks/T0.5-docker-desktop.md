# T0.5 — نصب Docker Desktop (WSL2)

| | |
|---|---|
| **فاز** | 0 — Foundation |
| **روز برنامه** | Day 3 (2026-08-31) |
| **وابستگی** | — |
| **مبحث Learn مرتبط** | Containers 101: Docker Desktop/WSL2 |
| **وضعیت** | ⬜ TODO |

## هدف
برای فازهای Qdrant/Langfuse/compose به Docker نیاز داریم؛ این تسک بستر ویندوزی اش را آماده می کند.

## گام‌ها
1. Docker Desktop را برای Windows (WSL2 backend) نصب کن.
2. `docker run hello-world` را اجرا کن.
3. `docker compose version` را هم چک کن.
4. اگر نصب ممکن نشد، این تسک را explicitly به روزهای بعد carry-over کن و بلاک را در PROGRESS بنویس (مانع ادامه کار نیست).

## فایل‌ها
- `(خارج از ریپو)`

## Verify
```bash
docker run hello-world
```
انتظار: پیام Hello from Docker!

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
