# T7.3 — مانیفست های K8s

| | |
|---|---|
| **فاز** | 7 — Hardening & Deploy |
| **روز برنامه** | Day 29 (2026-09-26) |
| **وابستگی** | T7.1 |
| **مبحث Learn مرتبط** | K8s primitives: Deployment, Service, Secret, probes |
| **وضعیت** | ⬜ TODO |

## هدف
استقرار پایه روی کلاستر: Deployment/Service/Secret + readiness و liveness probe.

## گام‌ها
1. deploy/k8s/: deployment+service برای api و worker؛ StatefulSet یا compose معادل برای pg/qdrant/redis (یا فرض سرویس مدیریت شده).
2. Secret ها جدا از کد (الگوی sealed/external در runbook).
3. probes: readiness /health/ready، liveness /health.
4. اعتبار سنجی با kubectl apply --dry-run=client (در صورت نبود کلاستر).
5. commit.

## فایل‌ها
- `deploy/k8s/`

## Verify
```bash
kubectl apply --dry-run=client -f deploy/k8s/
```
انتظار: validated بدون خطا

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
