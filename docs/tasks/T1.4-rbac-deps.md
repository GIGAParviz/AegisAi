# T1.4 — RBAC با FastAPI dependencies

| | |
|---|---|
| **فاز** | 1 — Gateway & Security |
| **روز برنامه** | Day 6 (2026-09-03) |
| **وابستگی** | T1.3 |
| **مبحث Learn مرتبط** | RBAC design + FastAPI dependency injection |
| **وضعیت** | ⬜ TODO |

## هدف
کنترل دسترسی نقش محور به صورت dependency قابل استفاده روی هر endpoint.

## گام‌ها
1. app/api/deps.py: get_current_user (از bearer token) و require_roles(*roles) که 403 برای نقش غیرمجاز برگرداند.
2. یک endpoint نمونه فقط-admin (مثل GET /admin/whoami) بساز تا الگو مستند شود.
3. tests/test_rbac.py: کاربر عادی → 403، admin → 200، بدون توکن → 401.
4. commit.

## فایل‌ها
- `app/api/deps.py`
- `tests/test_rbac.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_rbac.py -q
```
انتظار: 3 passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
