# T6.3 — دروازه کیفیت در CI

| | |
|---|---|
| **فاز** | 6 — Observability & MLOps |
| **روز برنامه** | Day 27 (2026-09-24) |
| **وابستگی** | T6.2 |
| **مبحث Learn مرتبط** | Regression gates in CI |
| **وضعیت** | ⬜ TODO |

## هدف
افت کیفیت RAG/پاسخ، build را قرمز کند تا رگرسیون پرامپت/مدل زود دیده شود.

## گام‌ها
1. اجرای evals در workflow (جاب جدا یا step بعد از pytest) با FakeProvider تا CI ارزان بماند.
2. آستانه ها: context_recall>=0.8 و faithfulness>=0.75 (قابل تنظیم در evals/thresholds.json).
3. خروجی گزارش به صورت artifact آپلود شود.
4. commit + تست با یک threshold سختگیرانه موقت که قرمز شود، بعد بازگردانی.

## فایل‌ها
- `.github/workflows/ci.yml`
- `evals/thresholds.json`

## Verify
```bash
git -C /e/projs/AegisAI push origin main  # جاب evals در Actions
```
انتظار: سبز؛ با آستانه مصنوعی قرمز می شود

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
