# T0.1 — تغییر نام ContextForge به AegisAI

| | |
|---|---|
| **فاز** | 0 — Foundation |
| **روز برنامه** | Day 1 (2026-08-29) |
| **وابستگی** | — |
| **مبحث Learn مرتبط** | Python packaging (pyproject.toml, uv, venv) |
| **وضعیت** | ✅ DONE (2026-08-31 تأیید — grep تمیز + کامیت) |

## هدف
شناسه داخلی پروژه در تمام فایل‌ها از ContextForge به AegisAI تغییر کند تا پلتفرم با نام اصلی‌اش شناخته شود.

## گام‌ها
1. در pyproject.toml مقدار name را به aegisai و description را به "Aegis AI reliability platform" تغییر بده (سطر description انگلیسی بماند).
2. در app/core/config.py مقدار پیش فرض app_name را به AegisAI تغییر بده.
3. عنوان و متن README.md را به AegisAI تغییر بده (بخش Tech Stack فعلا دست نخورد).
4. در .env و .example.env مقدار APP_NAME=AegisAI بگذار.
5. با دستور Verify مطمئن شو هیچ reference باقی نمانده، سپس commit.

## فایل‌ها
- `pyproject.toml`
- `app/core/config.py`
- `README.md`
- `.env`
- `.example.env`

## Verify
```bash
cd /e/projs/AegisAI && grep -ri contextforge . --include="*.py" --include="*.toml" --include="*.md" --include="*.env"
```
انتظار: خروجی خالی باشد (هیچ match)

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
