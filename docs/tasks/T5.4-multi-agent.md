# T5.4 — نمونه چند ایجنتی (planner/worker)

| | |
|---|---|
| **فاز** | 5 — Agentic Runtime |
| **روز برنامه** | Day 24 (2026-09-21) |
| **وابستگی** | T5.1، T5.2 |
| **مبحث Learn مرتبط** | Multi-agent patterns: planner/worker, handoffs |
| **وضعیت** | ⬜ TODO |

## هدف
الگوی handoff با ایجنت planner که کار را بین worker ها تقسیم می کند؛ gate در هر پرش.

## گام‌ها
1. app/agents/multi/planner.py و worker.py: subgraph ها با مسئولیت مشخص.
2. handoff: خروجی planner → صف وظایف → worker ها → تجمیع در supervisor node.
3. هر فراخوانی ابزار در worker هم از gate می گذرد.
4. tests/test_multi_agent.py: سناریوی دو مرحله ای با FakeProvider.
5. commit.

## فایل‌ها
- `app/agents/multi/planner.py`
- `app/agents/multi/worker.py`
- `tests/test_multi_agent.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_multi_agent.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
