# T7.4 — بازنویسی README + runbook

| | |
|---|---|
| **فاز** | 7 — Hardening & Deploy |
| **روز برنامه** | Day 30 (2026-09-27) |
| **وابستگی** | T7.1، T7.2 |
| **مبحث Learn مرتبط** | Incident runbooks (کوتاه) |
| **وضعیت** | ⬜ TODO |

## هدف
مستند نهایی: معماری، جریان داده، اجرای محلی، deploy، و runbook رخدادها.

## گام‌ها
1. README.md: نمودار معماری (ASCII یا mermaid)، quickstart، متغیرهای env، نحوه اجرای tests/evals.
2. docs/RUNBOOK.md: سناریوهای خرابی (redis down، qdrant down، LLM timeout) و اقدامات + پیوند به policy audit برای بررسی رخداد.
3. بستن رودمپ ۳۰ روزه: جمع بندی در PROGRESS.
4. commit نهایی.

## فایل‌ها
- `README.md`
- `docs/RUNBOOK.md`

## Verify
```bash
cd /e/projs/AegisAI && ls docs/RUNBOOK.md && git log --oneline | head -3
```
انتظار: فایل موجود + کامیت نهایی

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
