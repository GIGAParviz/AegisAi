# T0.3 — گسترش Settings + تست کانفیگ

| | |
|---|---|
| **فاز** | 0 — Foundation |
| **روز برنامه** | Day 2 (2026-08-30) |
| **وابستگی** | T0.2 |
| **مبحث Learn مرتبط** | pytest essentials: fixtures, parametrize, monkeypatch |
| **وضعیت** | ⬜ TODO |

## هدف
تنظیمات دیتابیس، Redis، Qdrant، LLM و JWT در pydantic-settings تعریف شود تا فازهای بعدی فقط از settings بخوانند.

## گام‌ها
1. در app/core/config.py این فیلدها را با default منطقی اضافه کن: database_url (sqlite+aiosqlite:///./aegis.db برای dev)، redis_url، qdrant_url، llm_provider، llm_base_url، llm_api_key، llm_model، jwt_secret، jwt_alg، access_token_expire_min.
2. tests/test_config.py بنویس: (۱) defaults درست parse می شوند؛ (۲) با monkeypatch.setenv مقدار env روی settings غلبه می کند (نمونه جدید Settings() بساز).
3. pytest را اجرا کن و سبز ببین، سپس commit.

## فایل‌ها
- `app/core/config.py`
- `tests/test_config.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_config.py -q
```
انتظار: 2 passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
