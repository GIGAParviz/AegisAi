# T3.1 — لایه انتزاع LLMProvider

| | |
|---|---|
| **فاز** | 3 — Reasoning Engine |
| **روز برنامه** | Day 14 (2026-09-11) |
| **وابستگی** | T0.3 |
| **مبحث Learn مرتبط** | OpenAI-compatible API surface + provider abstraction patterns |
| **وضعیت** | ⬜ TODO |

## هدف
قرارداد واحد برای هر مدل (llama.cpp محلی، vLLM آینده، API ریموت) تا تعویض مدل بدون تغییر بقیه سیستم ممکن باشد.

## گام‌ها
1. app/services/llm/provider.py: پروتکل LLMProvider با complete(messages, **kw) و stream(messages, **kw) (async generator).
2. OpenAICompatProvider با httpx.AsyncClient: POST /chat/completions با base_url از settings؛ پشتیبانی stream=True (SSE parsing).
3. factory از روی settings.llm_provider انتخاب کند (local/openai_compat).
4. tests/test_llm_provider.py: با fake transport (httpx.MockTransport): پاسخ کامل + جمع شدن stream chunks.
5. commit.

## فایل‌ها
- `app/services/llm/provider.py`
- `app/services/llm/__init__.py`
- `tests/test_llm_provider.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_llm_provider.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
