# T2.4 — سرویس Embedding با provider abstraction

| | |
|---|---|
| **فاز** | 2 — Data Engine |
| **روز برنامه** | Day 11 (2026-09-08) |
| **وابستگی** | T2.3 |
| **مبحث Learn مرتبط** | Embeddings: model choice, dims, cosine vs dot, batching |
| **وضعیت** | ⬜ TODO |

## هدف
لایه embedding که هم local (sentence-transformers) و هم remote API باشد و در تست ها fake بشود.

## گام‌ها
1. app/services/embeddings.py: پروتکل EmbeddingProvider.embed(texts)->vectors؛ پیاده سازی LocalSTProvider (مدل کوچک مثل bge-small یا all-MiniLM) و OpenAICompatProvider (httpx).
2. FakeEmbeddingProvider (بردار determinstic بر اساس hash) برای تست ها.
3. batch encoding با اندازه از settings.
4. tests/test_embeddings.py: fake provider ابعاد و determinism را چک کند.
5. commit.

## فایل‌ها
- `app/services/embeddings.py`
- `tests/test_embeddings.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_embeddings.py -q
```
انتظار: passed

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
