# T7.2 — Nginx reverse proxy

| | |
|---|---|
| **فاز** | 7 — Hardening & Deploy |
| **روز برنامه** | Day 29 (2026-09-26) |
| **وابستگی** | T7.1 |
| **مبحث Learn مرتبط** | Nginx: TLS termination, timeouts, gzip |
| **وضعیت** | ⬜ TODO |

## هدف
لایه پروکسی با TLS، gzip و timeout های مناسب SSE.

## گام‌ها
1. deploy/nginx/nginx.conf: upstream api؛ proxy_pass؛ برای /chat/stream: proxy_buffering off و timeout بلند.
2. TLS termination با self-signed برای dev + مستند تبدیل به certbot.
3. gzip برای json/html.
4. سرویس nginx در compose + تست SSE از پشت پروکسی.
5. commit.

## فایل‌ها
- `deploy/nginx/nginx.conf`
- `docker-compose.yml`

## Verify
```bash
curl -N http://localhost/chat/stream -X POST -H 'Content-Type: application/json' -d '{"messages":[{"role":"user","content":"hi"}]}'
```
انتظار: چانک های SSE بدون بافر شدن

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
