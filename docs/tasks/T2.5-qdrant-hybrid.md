# T2.5 — یکپارچه سازی Qdrant + جستجوی hybrid

| | |
|---|---|
| **فاز** | 2 — Data Engine |
| **روز برنامه** | Day 12 (2026-09-09) |
| **وابستگی** | T2.4 |
| **مبحث Learn مرتبط** | Day 12: مفاهیم Qdrant (collections, HNSW, named vectors) | Day 13: hybrid search و RRF |
| **وضعیت** | ⬜ TODO |

## هدف
ذخیره و جستجوی برداری با یک موتور: dense + sparse در یک collection و ترکیب نتایج با RRF.

## گام‌ها
1. app/services/vector_store.py: ensure_collection (named vectors: dense و sparse)، upsert_chunks (payload: doc_id, chunk_index, text, heading).
2. hybrid_search(query, k): جستجوی dense + sparse (sparse با fastembed/BM25 محلی) و fusion با RRF؛ بازگشت top-k با score و citation metadata.
3. task ingest_document را کامل کن: chunk→embed→upsert→status=ready.
4. tests: اگر Docker (T0.5) فعال است با Qdrant ephemeral؛ در غیر این صورت contract tests با fake store + یک integration test علامت گذاری شده با skip بدون docker.
5. commit.

## فایل‌ها
- `app/services/vector_store.py`
- `tests/test_vector_store.py`

## Verify
```bash
cd /e/projs/AegisAI && python -m pytest tests/test_vector_store.py -q
```
انتظار: passed (fake/ephemeral)

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
