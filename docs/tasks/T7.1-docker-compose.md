# T7.1 — docker-compose استک کامل

| | |
|---|---|
| **فاز** | 7 — Hardening & Deploy |
| **روز برنامه** | Day 28 (2026-09-25) |
| **وابستگی** | T0.5، T6.1 |
| **مبحث Learn مرتبط** | Compose networking, healthchecks, volumes |
| **وضعیت** | ⬜ TODO |

## هدف
کل پلتفرم با یک دستور بالا بیاید: api, worker, postgres, qdrant, redis, llama-server, langfuse.

## گام‌ها
1. Dockerfile چند مرحله ای برای api/worker (base slim، نصب با uv).
2. docker-compose.yml: سرویس ها + healthcheck (curl endpoints) + volumes برای pg/qdrant/redis + depends_on with condition.
3. env ها از .env.compose؛ llama-server با model mount.
4. تست: docker compose up -d و چک سلامت همه سرویس ها + /health.
5. commit.

## فایل‌ها
- `Dockerfile`
- `docker-compose.yml`
- `.env.compose.example`

## Verify
```bash
cd /e/projs/AegisAI && docker compose up -d && docker compose ps
```
انتظار: همه سرویس ها healthy

## یادداشت‌ها
- حالت وضعیتِ نهایی این تسک در docs/PROGRESS.md ثبت می شود (بازبینی ۲۱:۰۰).
