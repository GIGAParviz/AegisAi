APP_NAME=AegisAi
ENVIRONMENT=development
DEBUG=false

# Use `fake` for lightweight local development and CI. To use local
# sentence-transformers embeddings, install the `local-embeddings` extra and
# set this to `local`.
EMBEDDING_PROVIDER=fake
